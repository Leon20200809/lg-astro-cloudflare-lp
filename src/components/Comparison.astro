<section class="section section--muted comparison">
  <div class="container">
    <div class="section-header">
      <p class="section-label">今のままでいいか確認する</p>

      <h2 class="section-title">
        一度、ホームページを<br />
        整理してみませんか？
      </h2>

      <p class="section-lead">
        新しく作ることが目的ではありません。
        今あるものを確認し、必要なものと不要なものを整理します。
      </p>
    </div>

    <div class="comparison__content">
      <div class="comparison__panel">
        <div class="comparison__header">
          <span>今の状態</span>
          <span aria-hidden="true"></span>
          <strong>見直した後</strong>
        </div>

        <div class="comparison__row">
          <p>なんとなくWordPressを使っている</p>

          <span class="comparison__arrow" aria-hidden="true">
            →
          </span>

          <strong>目的に合った構成へ</strong>
        </div>

        <div class="comparison__row">
          <p>毎年サーバー料金を払い続けている</p>

          <span class="comparison__arrow" aria-hidden="true">
            →
          </span>

          <strong>
            0円にできる場合も
          </strong>
        </div>

        <div class="comparison__row">
          <p>システム更新や保守が必要</p>

          <span class="comparison__arrow" aria-hidden="true">
            →
          </span>

          <strong>必要な作業だけに</strong>
        </div>

        <div class="comparison__row">
          <p>誰が何を管理しているか分からない</p>

          <span class="comparison__arrow" aria-hidden="true">
            →
          </span>

          <strong>構成を整理して見える化</strong>
        </div>
      </div>

      <aside class="comparison__point">
        <span class="comparison__point-label">
          POINT
        </span>

        <div class="comparison__point-body">
          <h3>
            作り直す前に、<br />
            「本当に必要か」を確認します。
          </h3>

          <p>
            WordPressやレンタルサーバーを使っていること自体が
            問題なのではありません。
            御社の目的に対して、その構成が今も適しているかを確認します。
          </p>
        </div>
      </aside>
    </div>
  </div>
</section>

<style>
  .comparison__content {
    max-width: var(--content-width-lg);
    margin: var(--space-16) auto 0;
  }

  .comparison__panel {
    overflow: hidden;
    border: var(--border-default);
    border-radius: var(--radius-xl);
    background: var(--color-surface);
    box-shadow: var(--shadow-lg);
  }

  .comparison__header,
  .comparison__row {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      auto
      minmax(0, 1fr);
    gap: var(--space-8);
    align-items: center;
  }

  .comparison__header {
    padding: var(--space-5) var(--space-8);
    color: var(--color-text-muted);
    background: linear-gradient(90deg, var(--color-surface-soft) 0 48%, var(--color-accent-soft) 48% 100%);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
  }

  .comparison__header strong {
    color: var(--color-accent);
  }

  .comparison__row {
    min-height: 6rem;
    padding: var(--space-6) var(--space-8);
    border-top: var(--border-default);
    background: linear-gradient(90deg, var(--color-surface) 0 48%, #f7fbff 48% 100%);
  }

  .comparison__row p {
    margin: 0;
    color: var(--color-text-muted);
  }

  .comparison__row strong {
    color: var(--color-text);
    font-size: var(--font-size-md);
    font-weight: var(--font-weight-bold);
    padding-left: var(--space-2);
  }

  .comparison__arrow {
    display: grid;
    width: 2.75rem;
    height: 2.75rem;
    place-items: center;
    border-radius: var(--radius-full);
    color: var(--color-accent);
    background: var(--color-accent-soft);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-heavy);
  }

  .comparison__point {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-6);
    align-items: start;
    margin-top: var(--space-10);
    padding: var(--space-10);
    border-radius: var(--radius-xl);
    color: var(--color-primary-text);
    background: linear-gradient(135deg, var(--color-primary), #122f53);
    box-shadow: var(--shadow-lg);
  }

  .comparison__point-label {
    padding: var(--space-2) var(--space-3);
    border: 1px solid rgb(255 255 255 / 0.16);
    border-radius: var(--radius-full);
    color: #93c5fd;
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-heavy);
    letter-spacing: 0.08em;
  }

  .comparison__point-body {
    max-width: var(--content-width-md);
  }

  .comparison__point h3 {
    margin: 0;
    font-size: var(--font-size-xl);
    line-height: var(--line-height-heading);
  }

  .comparison__point p {
    margin: var(--space-3) 0 0;
    color: rgb(255 255 255 / 0.7);
    font-size: var(--font-size-sm);
  }

  @media (max-width: 40rem) {
    .comparison__content {
      margin-top: var(--space-12);
    }

    .comparison__header {
      display: none;
    }

    .comparison__row {
      grid-template-columns: 1fr;
      gap: var(--space-3);
      min-height: auto;
      padding: var(--space-6);
      background: var(--color-surface);
    }

    .comparison__arrow {
      width: 2.25rem;
      height: 2.25rem;
      transform: rotate(90deg);
    }

    .comparison__point {
      grid-template-columns: 1fr;
      gap: var(--space-4);
      padding: var(--space-6);
    }

    .comparison__row strong {
      padding: var(--space-3) var(--space-4);
      border-radius: var(--radius-sm);
      background: var(--color-accent-soft);
    }
  }
</style>
