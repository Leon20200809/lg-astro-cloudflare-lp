{
  "name": "lg-astro-cloudflare-lp",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "wrangler deploy --dry-run --outdir .test-dist && node --test tests/contact-worker.test.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
