<section class="section section--surface solution">
  <div class="container">
    <div class="section-header">
      <p class="section-label">必要なものだけ残す</p>

      <h2 class="section-title">
        シンプルなホームページへ。
      </h2>

      <p class="section-lead">
        更新頻度が低い会社案内サイトなら、
        使っていない機能や複雑な仕組みまで維持する必要はないかもしれません。
      </p>
    </div>

    <div class="solution__grid">
      <article class="solution-card">
        <div class="solution-card__icon">
          <img
            src="/icons/speed.svg"
            alt=""
            width="48"
            height="48"
          />
        </div>

        <h3>表示が速い</h3>

        <p>
          必要な情報を軽い構成で届け、
          閲覧する人を待たせにくいホームページへ。
        </p>
      </article>

      <article class="solution-card">
        <div class="solution-card__icon">
          <img
            src="/icons/management.svg"
            alt=""
            width="48"
            height="48"
          />
        </div>

        <h3>管理するものが少ない</h3>

        <p>
          使わない仕組みを減らし、
          「何を管理すればいいのか」が分かる状態にします。
        </p>
      </article>

      <article class="solution-card">
        <div class="solution-card__icon">
          <img
            src="/icons/maintenance.svg"
            alt=""
            width="48"
            height="48"
          />
        </div>

        <h3>保守の手間を減らす</h3>

        <p>
          維持するためだけの更新作業を減らし、
          本当に必要な仕事へ時間を使えるようにします。
        </p>
      </article>

      <article class="solution-card solution-card--accent">
        <div class="solution-card__icon">
          <img
            src="/icons/cost.svg"
            alt=""
            width="48"
            height="48"
          />
        </div>

        <h3>費用を削減できる場合も</h3>

        <p>
          サイトの用途によっては、
          毎年支払っているWebサーバー費用をなくせる場合もあります。
        </p>
      </article>
    </div>

    <div class="solution__note">
      <strong>WordPressを否定するサービスではありません。</strong>

      <p>
        WordPressが適している会社には、そのまま残します。
        技術ありきではなく、御社の目的から必要な構成を判断します。
      </p>
    </div>
  </div>
</section>

<style>
  .solution__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    column-gap: var(--space-16);
    row-gap: 0;
    max-width: var(--content-width-lg);
    margin: var(--space-16) auto 0;
  }

  .solution-card {
    display: grid;
    grid-template-columns: 4rem 1fr;
    grid-template-rows: auto 1fr;
    min-height: 14rem;
    padding: var(--space-10) 0;
    border: 0;
    border-bottom: var(--border-default);
    border-radius: 0;
    background: var(--color-surface);
    box-shadow: none;
  }

  .solution-card--accent {
    background: transparent;
  }

  .solution-card__icon {
    display: flex;
    grid-row: 1 / 3;
    width: 3rem;
    height: 3rem;
    align-items: center;
  }

  .solution-card__icon img {
    width: 2.5rem;
    height: 2.5rem;
    object-fit: contain;
  }

  .solution-card h3 {
    margin: 0 0 var(--space-3);
    font-size: var(--font-size-xl);
  }

  .solution-card p {
    margin: 0;
    color: var(--color-text-muted);
  }

  .solution__note {
    max-width: var(--content-width-md);
    margin: var(--space-12) auto 0;
    padding: var(--space-6) var(--space-8);
    border: var(--border-default);
    border-radius: var(--radius-md);
    background: var(--color-surface-soft);
    text-align: center;
  }

  .solution__note strong {
    display: block;
    font-size: var(--font-size-md);
  }

  .solution__note p {
    margin: var(--space-2) 0 0;
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
  }

  @media (max-width: 40rem) {
    .solution__grid {
      grid-template-columns: 1fr;
      margin-top: var(--space-12);
      gap: 0;
    }

    .solution-card {
      min-height: auto;
      grid-template-columns: 3.5rem 1fr;
      padding: var(--space-8) 0;
    }

    .solution__note {
      padding: var(--space-5);
    }
  }
</style>
