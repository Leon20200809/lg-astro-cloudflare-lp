<section class="section section--surface solution">
  <div class="container">
    <div class="section-header">
      <p class="section-label">必要なものだけ残す</p>

      <h2 class="section-title">
        シンプルなホームページへ。
      </h2>

      <p class="section-lead">
        更新頻度が低い会社案内サイトなら、
        複雑な仕組みを持たなくてもよい場合があります。
      </p>
    </div>

    <div class="solution__grid">
      <article class="card">
        <h3>表示が速い</h3>
        <p>必要な情報を、できるだけ軽くシンプルに届けます。</p>
      </article>

      <article class="card">
        <h3>管理するものが少ない</h3>
        <p>使わない仕組みを減らし、管理の負担を小さくします。</p>
      </article>

      <article class="card">
        <h3>更新の手間を減らせる</h3>
        <p>維持のためだけに必要な作業を減らします。</p>
      </article>

      <article class="card">
        <h3>費用を削減できる場合も</h3>
        <p>構成によっては、毎年のサーバー費用も抑えられます。</p>
      </article>
    </div>
  </div>
</section>

<style>
  .solution__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
    max-width: var(--content-width-lg);
    margin: var(--space-12) auto 0;
  }

  .solution h3 {
    margin: 0 0 var(--space-2);
    font-size: var(--font-size-xl);
  }

  .solution article p {
    margin: 0;
    color: var(--color-text-muted);
  }

  @media (max-width: 40rem) {
    .solution__grid {
      grid-template-columns: 1fr;
    }
  }
</style>